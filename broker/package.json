{
  "name": "boxlite-token-broker",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker: GitHub Actions OIDC → scoped @boxlite[bot] installation token",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@octokit/auth-app": "^7"
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}
